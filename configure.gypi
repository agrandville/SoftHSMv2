{
  'variables': {
	'build_with_botan' : '1',
	'build_with_openssl' : '0',
	# Compile with GOST support
	'enable_ghost' : '1',
	# Compile with ECC support
	'enable_ecc' : '1',
	# Define if advanced AES key wrap without pad is supported
	'enable_aes_key_wrap' : '1',
	# Debug to stderr
	'enable_debug_to_stderr' : '1'
  }
}